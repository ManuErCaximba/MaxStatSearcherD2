import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'max-stat-searcher-d2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
